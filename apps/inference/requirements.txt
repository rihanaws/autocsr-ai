# Pinned to versions actually installed in the dev pyenv (3.11.9) and
# verified to exist on PyPI — previous pins were never validated and broke
# the Railway build (upstash-vector==1.1.6 does not exist).
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4

# LangGraph
langgraph==1.2.4
langchain-openai==1.2.2

# AI clients
openai==2.41.0
anthropic==0.105.2

# Upstash Vector (semantic cache + knowledge retrieval)
upstash-vector==0.8.0

# HTTP
httpx==0.28.1

# Database (direct writes from Python — no Prisma)
asyncpg==0.31.0

# Auth (QStash JWT verification)
PyJWT==2.13.0

# Utilities
python-dotenv==1.2.2
